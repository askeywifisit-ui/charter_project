# 2026-02-10

- 使用者提供：charter 自動化平台專案資料在 `/Users/da40_llmcda40da40/.openclaw/chater_project`。
  - `system/`：web/api 程式碼；`tools.zip` 工具模組。
  - `docs/`：topology、test plan。
  - `script/`：sanity、stability。
- 平台 IP：`172.14.1.140`，UI：`http://172.14.1.140:5173`。
- 已產出：API Catalog、Selenium 控制點清單、Control Matrix。

## 今日操作
- 清理 runs：stop active + purge finished（older_than_days=0）。
- 全刪 scripts，並匯入/修改/執行 sanity + stability 代表腳本；確認 run/log 下載 API 可用。

## 腳本優化/偏好
- 使用者偏好：CPE reboot 期間 console/serial mute **約 120 秒即可**（避免 boot load lock）。
- 已在 workspace 版本 manifest 調整：
  - `C15807173_Test_Upnp_discovery/manifest.yaml`：REBOOT_MUTE_SECS=120（保留 REBOOT_MUTE_WITH_LOCK=1）
  - `C00000001_SSH_basic_test/manifest.yaml`：REBOOT_MUTE_SECS=120

## 今日新增（記憶/KB/排程/測試執行）
### KB（Project Knowledge Base）
- 已將專案包升級為專業 KB：`workspace/docs/charter_project/`
  - 入口：`00_overview/README.md`
  - 換 session：`00_overview/session_bootstrap.md`
  - Topology：`01_topology/topology.md`（引用 `chater_project/docs/charter-topology-physical.png`, sanity/stability PDF）
  - Runbook：`06_runbook/runbook.md`
  - Script 重點：`05_scripts/C15807173_upnp_discovery.md`
- 舊檔保留（redirect 形式）：`docs/charter_project/README.md`、`runbook.md`、`session_bootstrap.md`…

### 記憶打包（可搬到第二台）
- 已打包：`workspace/charter_memory_pack_20260210_111750.tar.gz`
  - 內容含：`MEMORY.md`、`memory/`、`docs/charter_project/`、以及 `docs/api_catalog.md`/`selenium_control_points.md`/`control_matrix.md`

### 測試執行策略（無 IPv6）
- 因環境無 IPv6，以下三支 **不要 run**（以 name 排除；但仍要備份）：
  - `C4123494_IPv6_Stability_Test_from_LAN_Client`
  - `C15807061_WAN_IPv6_Connectivity`
  - `C15807139_SSH_should_be_allowed_to_WAN_from_operator_facing_client`
- 其餘 scripts 設定為 run **1 cycle**：
  - 用 `set_cycles_and_make_runner.py` 對「非排除」腳本寫回 manifest：`env.CYCLES='1'`
  - 產出 run 清單：`workspace/runlist_excluding_ipv6.json`（35 支）

### 逐一執行（sequential runner）
- runner：`workspace/run_sequential_excluding_ipv6.py`（依 runlist 逐一 run + poll status）
- 結果摘要檔：`workspace/sequential_run_summary.json`
- 起跑時第一支：`stability:C6376350_WiFi_radio_enable_disable-xtime` run_id=6439（running）

### Cron 排程（進度/報表）
- 每 10 分鐘進度更新（測試停止/結束則自動移除）：jobId=`87705084-0e81-41d5-8819-593bf015ea02`
- 每天 09:30（台北時間）近 3 天報表提醒：jobId=`cdce4406-2609-46f6-b7f2-ab1fa695b3de`

### Review / 優化政策（DA40 指示）
- 我會先 review 測試 log/代碼，把可優化點與一致性問題先記成 backlog，再逐步優化。
- **任何優化/改動前必須先備份腳本與程式碼**（平台 scripts export、workspace 記憶/KB 打包、system zips/解包目錄打包）。
- KB 文件：`workspace/docs/charter_project/06_runbook/review_and_optimization_policy.md`

### SSH 控制（control PC）
- DA40 已完成將 SSH key 複製到 Charter control PC：`172.14.1.140`（以 root 登入可用），後續可用 SSH 直接控管/修改。
- 主要維護路徑（control PC 上）：
  - tools 模組：`/home/da40/charter/tools`
  - web：`/home/da40/charter/apps/web`
  - api（app root）：`/home/da40/charter/apps/api/app/`

### 雲端同步（Google Drive Mirror）— 專案來源快照
- 已從 control PC 將 tools/web/api 重新打包成 zip 並更新本機：`chater_project/system/{tools,web,api}.zip`
  - 更新前備份：`chater_project/system/backup_20260210_123737/`
- 已建立專案快照包（含 system/docs/script）：
  - `workspace/charter_project_snapshot_20260210_123806.tar.gz`
  - 已複製到 Google Drive 同步資料夾：`~/Documents/OpenClaw_Charter_KB/project_snapshots/`
- 已新增一鍵同步腳本（下次直接跑就會：更新 zip + 產生 snapshot + 產生 memory pack + 丟雲端）：
  - `workspace/sync_controlpc_and_pack_to_gdrive.sh`

### 腳本版本管理（避免混淆）
- suite 目錄（sanity/stability）只保留一份正式 zip；變體/備份移到 `script/backup/<timestamp>/`。
- 已將 `sanity/C15807173_Test_Upnp_discovery_optimized_onoff.zip` 移至：`script/backup/20260210_155001_manual/`
