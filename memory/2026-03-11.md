# 2026-03-11

## Charter 文件站（100.40.1.115:8000）大幅優化與交付化

### 文件站服務穩定化
- 釐清「新/舊版」來源混亂：mkdocs 原本可能在舊目錄 `workspace/docs/charter_site` 起服務。
- 統一 single source of truth：`workspace/charter_delivery_repo/docs/charter_site`。
- 調整 `/Users/csit/bin/start_charter_docs.sh`：
  - 固定 serve `charter_delivery_repo/docs/charter_site`（移除 legacy fallback）。
  - mkdocs bind 改為 `0.0.0.0:8000` 以便同事用 `http://100.40.1.115:8000/` 存取。
  - 強化 kill 舊 mkdocs / 釋放 port；加入內容 marker 檢查，避免誤以為更新成功但其實跑舊站。
  - 偵測 cloudflared quick tunnel 1015/429 rate limit 時直接提示「請稍後再試」，避免越試越被限流。
- trycloudflare quick tunnel 不穩且會被 1015/429；內部改以固定 IP `http://100.40.1.115:8000/` 為主。

### Getting Started：平台安裝（platform_install）
- 改成同事角度的專業 SOP：
  - 明確成功標準（API / web 同源 API / worker）。
  - 平台組件表（API/Worker/Web/Metrics/Probe/Watchdog）並改成「全部必需」。
  - 新增「一鍵安裝 block」：OS 依賴、解壓到 `/home/da40/charter`、DB rg/rg、units enable --now、health check。
  - 交付檔案路徑範例統一到 `/home/da40/charter/packages/`，並用具體檔名（不讓同事猜 <ts>）。
  - 移除重複的 downloads 提示。
  - 頁首加註「給 OpenClaw 測試助理：可依此頁協助安裝/移植」。

### Getting Started：Quick Start
- 強化為同事可直接照做：
  - 加入 11 LAB 範例（含 smoke case 查 script_id 的 python 片段）。
  - worker 失敗時補 `journalctl -u charter-worker.service`。

### Environment Template（換環境必讀）
- 新增「參數要填到哪裡」對照表：systemd vs `.secrets/dut.env` vs `noc_profiles.json` vs manifest defaults。
- 加入 11F_140 可 copy/paste 範本（admonition 卡片）：
  - A) systemd drop-in override（worker env）
  - B) `.secrets/dut.env` template（加 key：WAREHOUSE_ID=ThylacineGone、WAREHOUSE_PASSWORD/SSH_PASSWORD 用 <fill>）並說明 secrets precedence（dut.env 優先，manifest 只 defaults）。
  - C) `noc_profiles.json` template（不放真值）。
- A) 內的多個 PS（iface/PDU/serial）改為可折疊 `??? note`，避免頁面太長。
- 在 PDU 段落加連結導到 tools/pdu。

### Tools：PDU 文件專業化
- 實際查看 11F_140 tools：
  - `pdu_outlet1.py` brand：MSNSwitch/UIS（HTTP API，/api/status /api/control）。
  - `pdu_outlet2.py` brand：WEBPOWERSWITCH.COM（DLI Web Power Switch）。
- 重寫 `tools/pdu.md`：列出已支援品牌、設定點、CLI、移植注意事項、以及如何擴充新增 `pdu_outletX.py`。

### User Guide：三頁串成一條龍 SOP
- 採用方案 A：保留三頁但新增入口頁。
- 新增 `user_guide/script_change_sop.md`（入口 SOP）：Export→Modify→Delete same name→Import2→Run→Evidence。
- `scripts.md / modify_script_code.md / runs.md` 互相 cross-link；runs.md 補成可用 curl 範例。
- script_change_sop 頁首加註「給 OpenClaw 測試助理可依此控管平台」。
- 強化 script_change_sop 的人性/專業：成功標準、ID 差異、刪除風險、zip 結構檢查。

### 全站連結修正
- 修正多處 `.../runs/`、`.../scripts/` 等相對連結，統一改成 `.md` 或正確 nav link，減少 mkdocs build warnings。

## 風險/資安提醒（實機）
- controlpc 上 `dut.env` 出現明文：`WAREHOUSE_PASSWORD`、`SSH_PASSWORD`；文件站僅提供 `<fill>` template，不放真值。

