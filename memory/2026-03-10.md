# 2026-03-10

## Charter 文件站 / GitHub 交付整理（11F_140）

- Review 文件站：確認「平台程式長駐/重開機自動恢復」說明在 `getting_started/platform_install`（systemd units / enable）。
- 交付 repo：`askeywifisit-ui/charter_project`（GitHub）。

## 敏感資訊外洩修復（NOC/SSH/Warehouse 等）

- 發現問題：
  - tools 交付包內存在明文 NOC 帳密（`NOC_EMAIL=c-xin.guo@charter.com`, `NOC_PASSWORD=Xin@Plume`）於 `tools/upnp.status` 與 `tools/maint_only/*`。
  - 文件站 build 輸出 `docs/charter_site/site/` 內的 HTML 也烘焙了敏感值（包含 NOC/SSH/Warehouse 類）。
  - 部分 scripts zip 的 `manifest.yaml` 也帶有 NOC_EMAIL 明文。

- 措施：
  1) 文件站：
     - 將 docs 中敏感欄位改為 `<fill>`/空值。
     - **移除 `docs/charter_site/site/` 並加入 `.gitignore`**（避免 build artifact 進 repo）。
  2) scripts：
     - 批次修改 `scripts/**/*.zip` 內 `manifest.yaml`，將 `NOC_EMAIL/NOC_PASSWORD/SSH_PASSWORD/...` scrub 成 `<fill>`。
     - 強化 `scripts/sanitize_manifests_in_zips.py`：新增 keyed scrub `NOC_EMAIL` + 修復意外 regex artifact `\g<1>"0"`。
  3) tools：
     - `upnp.status` 改為從 env / `.secrets`（`SECRETS_FILE=/home/da40/charter/.secrets/noc_env.sh`）讀取；預設 `<fill>`，不再寫死。
     - `maint_only/test_upnp_cli.sh`、`maint_only/verify_upnp_realized.sh` 同樣改讀 env；另把工具 usage 範例的 `--password 123456789` 改為 `<fill>`。
  4) **History rewrite**：
     - 依使用者要求做 git history rewrite（`git-filter-repo`），移除舊版 site/build 與舊 tar.gz/zip 等 binary 歷史，並 replace 既知敏感字串。
     - 先建立 bundle 備份：`workspace/_safety_backups/charter_project_pre_history_rewrite_20260310_201048.bundle`。
     - Force push 回 `main`，並將「已消毒」的 packages/scripts 重新加回最新 commit。

- 文件站下載頁調整：
  - `handoff/downloads.md` 先移除 Tools 的公開下載 link（改成內部管道）。

## 11F_140 最新腳本同步 + 文件站全量更新

- 透過 11F_140 平台 API：`http://172.14.1.140:5173/api/scripts` 確認 scripts 數量：sanity 28 / stability 11。
- 新增 `scripts/sync_scripts_from_11F_140.py`：從 11F_140 export zip 同步到 repo 並更新 docs 內 11F_140 script id。
- 新增 `scripts/generate_docs_from_11F_140_zips.py`：從最新 export zip 的 manifest.yaml **重新產生**所有 test case 說明頁（docs/test_suites/sanity|stability）。
- 完成全量重生：39 個 case 頁（sanity+stability），並掃描確保不含 `@charter.com` 或 NOC 明文。

## 平台安裝文件（同事可照做）

- 重寫 `getting_started/platform_install.md` 成 step-by-step SOP：依賴、解壓路徑、DB、secrets、systemd enable/順序、health check。
- 依使用者偏好：DB 預設可寫明 `rg/rg`（lab default），並確認文件中不出現 NOC 帳密。

## 其他

- GitHub 可見性判斷：未登入 web_fetch 看到 404，推測 repo 為 private 或需權限（因 git push 正常）。

## 文件站可讀性優化（同事友善）

- 先備份文件站資料：`backups/docs_site/charter_site_docs_pre_opt_20260310_230450.tar.gz`（已 commit）。
- 全量改寫 sanity/stability test case 頁面格式：加入「一句話說明 / Preconditions / Top env / Run / Evidence / Cleanup」，並將完整 manifest 變成可折疊區塊（同事照做即可）。
- 更新入口頁：
  - `getting_started/quick_start.md` → 10 分鐘確認平台可用（health/worker/run/log/purge）。
  - `handoff/index.md`、`handoff/handoff_validation.md`、`tools/index.md` → 同事照做版。
- `platform_install.md` 增加「安裝完成後：環境配置（必做）」連結串接到 env template / NOC profile / env form / iface guide / quick start / validation。

## 實機環境配置補強（11F_140 vs 11F_131）

- 直接 SSH 進 `172.14.1.140` 與 `172.14.1.131` 對照 systemd units 與 iface/serial：
  - 11F_140：`LAN_PARENT_IFACE=eno2`、`PING_IFACE=eno2`、`WIFI_IFACE=wlx6cb0ce1ff230`、`CPE_DEV=/dev/serial/by-id/...->ttyUSB0`。
  - 11F_131：`LAN_PARENT_IFACE=enx245ebe6507f7`、`PING_IFACE=enx245ebe6507f7`、`WIFI_IFACE=wlan0`、`CPE_DEV=/dev/ttyUSB0`，且 `pbr-watchdog` 有 drop-in override 設定 IF/SRC/GW/SUB。
- 文件站補強頁面：
  - `handoff/network_iface_guide.md` 加入 11F_140/131 對照表。
  - `environment_template.md` 增加實機參考值提示。
  - `handoff/noc_profile_secrets.md` 補強 profiles 路徑/欄位（不含 email/password）。
- `.gitignore` 原本忽略 `*secret*` 造成 `noc_profile_secrets.md` 不能進版控；已新增 exception 允許此概念文件被 track（仍不會 track 真正 secrets 檔）。

