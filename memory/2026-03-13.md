
## 2026-03-13 後續（文件站與 Skills 大幅調整）
- 文件站（charter_site）優化：
  - Quick Start：新增「自動查 script_id 再 run」段落，避免手抄 ID 錯誤；script_change_sop 全面改為「先存 /tmp/scripts.json 再 python 解析」避免 curl|python heredoc broken pipe；加入 dry-run 安全檢查清單與刪除前確認；將「給 OpenClaw 測試助理」改為 tip 高亮；關掉全站標題 permalink（¶）。
  - Platform Links / README：改成以 Charter 測試平台為主體（文件站只是呈現方式）；Getting Started nav 順序調整（platform_install → quick_start）。
  - platform_install 一鍵安裝補強：加入 user/交付物前提檢查、Node/pnpm、.secrets（dut.env、noc_profiles.json）說明、systemd units 解壓目錄說明與驗證指令、驗收加入 systemctl status / 目錄/DB conninfo。
  - test_suites：說明用語「去敏」改成「流程參數完整；帳密/API key 已遮蔽」；每個 case 加『來源與下載』區塊（GitHub scripts 目錄 + Export zip 指令）；曾嘗試加文件站 assets 直下載 zip，後依使用者需求已移除『腳本 zip（直接下載）』連結。
  - Tools 頁面一度 404：根因是同時跑兩個 mkdocs serve 導致 tunnel 指到錯的站；已停掉舊的 127.0.0.1:8000 serve，重啟正確 repo，Cloudflare 端恢復 200。
- 新增 Skill：`cloudflare-quick-tunnel`（workspace skills）
  - 用途：查詢/必要時 refresh trycloudflare quick tunnel URL；預設 cooldown（避免頻繁 re-get 被 Cloudflare 擋）。
  - 位置：`~/.openclaw/workspace/skills/cloudflare-quick-tunnel/`；可用指令輸出當前 URL。
- Cloudflare tunnel：可重啟並產生新網址；近期可用 URL 例：`https://classic-applicable-defining-nuke.trycloudflare.com`（會變）。
